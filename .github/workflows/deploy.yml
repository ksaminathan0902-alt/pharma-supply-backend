name: Deploy Inventory Backend

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Copy files to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "src/,prisma/,package.json,package-lock.json"
          target: /home/ubuntu/inventory-backend
          strip_components: 0

      - name: Install, migrate and restart
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/ubuntu/inventory-backend

            # Setup DB and dedicated user via sudo mysql
            sudo mysql <<'SQLEOF'
CREATE DATABASE IF NOT EXISTS hms_inventory CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER IF NOT EXISTS 'inv_user'@'localhost' IDENTIFIED BY 'Inv@2026!';
GRANT ALL PRIVILEGES ON hms_inventory.* TO 'inv_user'@'localhost';
FLUSH PRIVILEGES;
SQLEOF

            # Always write .env with correct credentials
            cat > .env <<'ENVEOF'
DATABASE_URL=mysql://inv_user:Inv@2026!@localhost:3306/hms_inventory
JWT_SECRET=inventory_super_secret_jwt_2026
PORT=5001
ENVEOF

            npm install --omit=dev
            npx prisma generate
            npx prisma migrate deploy || true

            pm2 describe hms-inventory-backend > /dev/null 2>&1 \
              && pm2 restart hms-inventory-backend \
              || pm2 start src/index.js --name hms-inventory-backend

            pm2 save

            sleep 2
            curl -sf http://localhost:5001/api/health && echo "✅ Inventory backend deployed OK"
